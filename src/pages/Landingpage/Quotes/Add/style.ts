import { makeStyles } from "@material-ui/core";

export const useStyles = makeStyles({
    container: {
        width: "50%",
        display: "grid",
        rowGap: "8px",
        gridTemplateColumns: "1fr",
        marginBottom: "4px",
    },
  });