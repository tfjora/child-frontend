import { makeStyles } from '@material-ui/core/styles';

export const useStyles = makeStyles({
    container: {
        boxSizing: 'border-box',
        height: '100%',
        padding: '32px',
        width: '100%',
    },
});
